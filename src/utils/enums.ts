export const enum RespMsg {
  OK = 'ok',
  FAIL = 'fail'
}

export const enum Method {
  GET = 'get',
  POST = 'post',
  PUT = 'put',
  DELELTE = 'delete'
}

export const enum WhereKey {
  USERNAME = 'username',
  USER_ID = 'userId'
}
